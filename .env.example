# Database Configuration
# Choose one of the following database options and uncomment the relevant section

# Option A: SQLite (simplest - no server needed)
DATABASE_URL="file:./appointments.db"

# Option B: PostgreSQL
# DATABASE_URL="postgresql://username:password@localhost:5432/linanails"

# Option C: MySQL
# DATABASE_URL="mysql://username:password@localhost:3306/linanails"

# Option D: MongoDB
# MONGODB_URI="mongodb://localhost:27017/linanails"

# WhatsApp Business API Configuration
# These are required for sending WhatsApp messages directly through the API
# If not provided, the app will fall back to opening WhatsApp in the browser
# For Vite applications, client-side environment variables must be prefixed with VITE_

# The phone number ID should be obtained from your Meta Developer Dashboard
# It's not the same as your actual phone number - it's a unique identifier for your WhatsApp Business account
# Format: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/phone-numbers
VITE_WHATSAPP_PHONE_NUMBER_ID="your_whatsapp_business_phone_number_id"

# Your WhatsApp Business API access token from the Meta Developer Dashboard
VITE_WHATSAPP_ACCESS_TOKEN="your_whatsapp_business_api_access_token"

# The WhatsApp API version to use (default is v17.0 if not specified)
VITE_WHATSAPP_API_VERSION="v17.0"